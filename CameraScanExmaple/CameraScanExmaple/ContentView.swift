import SwiftUI
import CameraScan

struct ContentView: View {
    var body: some View {
      CameraScanView()
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}
